import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#00A6A4'
    },
    secondary: {
      main: '#d1a435'
    }
  },
  typography: {
    //  allVariant
  }
});

export default theme;
